#ifndef MENU_HPP_INCLUDED
#define MENU_HPP_INCLUDED

#include "collection.hpp"

class Menu {
    private:

    public:
        Menu();
        void showMenu(Collection& collection);
    };

#endif // MENU_HPP_INCLUDED
